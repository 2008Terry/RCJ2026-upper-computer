# Obsolete / Unused Files

This directory archives launch files and ROS node sources that are no longer part
of the active rcj_localization runtime.

Files here are intentionally outside the normal `launch/`, `src/`, and `scripts/`
paths so they are not built or installed as active package entry points.
